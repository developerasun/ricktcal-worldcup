name: Hourly cron job
on:
  schedule:
    - cron: '*/60 * * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Hourly cron job
        run: |
          curl --request POST \
          --url 'GET https://demo.developerasun.dpdns.org/api/webhook' \
          --header 'Authorization: Bearer ${{ secrets.BEARER_TOKEN }}'
